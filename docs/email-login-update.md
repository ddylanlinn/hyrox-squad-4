# Email 登入更新說明 ✅

## 📋 更新摘要

已成功將**匿名登入**替換為 **Email/密碼登入**，使用固定密碼 `Hyrox20260228`。

---

## 🎯 變更內容

### **移除功能**

- ❌ 匿名登入（Guest Mode）

### **新增功能**

- ✅ Email/密碼登入
- ✅ 固定密碼：`Hyrox20260228`
- ✅ 自動註冊：首次使用的 Email 會自動建立帳號
- ✅ 使用者只需輸入 Email，密碼自動填入

---

## 🔐 登入方式

### **方式 1：Google 登入**

```
1. 點擊 "login with google"
2. 選擇 Google 帳號
3. 完成登入
```

### **方式 2：Email 登入**

```
1. 輸入 Email（例如：dylan@example.com）
2. 點擊 "login with email"
3. 系統自動使用密碼：Hyrox20260228
4. 完成登入
```

---

## 🔄 自動註冊流程

當使用者第一次使用某個 Email 登入時：

```
1. 使用者輸入：dylan@example.com
2. 系統嘗試登入 → 使用者不存在
3. 自動建立帳號：
   - Email: dylan@example.com
   - Password: Hyrox20260228
   - Display Name: dylan (Email 前綴)
4. 登入成功
5. 顯示使用者選擇畫面（Dylan/Crystal/Sylvi/Andrew）
```

---

## 📝 技術實作

### **1. LoginView.vue**

- 移除匿名登入按鈕
- 新增 Email 輸入表單
- 新增分隔線（"or"）
- 固定密碼常數：`FIXED_PASSWORD = "Hyrox20260228"`

### **2. Auth Service**

```typescript
// src/services/auth/index.ts

export async function signInWithEmail(
  email: string,
  password: string
): Promise<FirebaseUser> {
  try {
    // 嘗試登入
    const result = await signInWithEmailAndPassword(auth, email, password);
    return result.user;
  } catch (error: any) {
    // 如果使用者不存在，自動註冊
    if (error.code === "auth/user-not-found") {
      const displayName = email.split("@")[0];
      return await signUpWithEmail(email, password, displayName);
    }
    throw error;
  }
}
```

### **3. useAuth Composable**

- 移除 `signInAnonymously` 方法
- 移除相關的 import 和 interface 定義

---

## 🎨 UI 更新

### **登入頁面**

```
┌─────────────────────┐
│   HYROX             │
│   login to start    │
│                     │
│  [Google Login]     │  ← Google 登入
│                     │
│  ─────── or ───────  │  ← 分隔線
│                     │
│  Email              │
│  [your@email.com]   │  ← Email 輸入框
│                     │
│  [login with email] │  ← Email 登入按鈕
└─────────────────────┘
```

---

## 🧪 測試步驟

### **測試 1：首次 Email 登入**

```
1. 開啟應用
2. 輸入新的 Email：test@example.com
3. 點擊 "login with email"
4. 應該看到：
   - Console: "User not found, creating new account..."
   - 自動建立帳號並登入
   - 顯示使用者選擇畫面
5. 選擇一個使用者（例如 Dylan）
6. 進入應用
```

### **測試 2：再次 Email 登入**

```
1. 登出
2. 輸入相同的 Email：test@example.com
3. 點擊 "login with email"
4. 應該看到：
   - 直接登入成功
   - 顯示使用者選擇畫面（如果之前已綁定則直接進入）
```

### **測試 3：錯誤密碼**

```
注意：使用者無法輸入密碼，所以不會有密碼錯誤的情況
固定密碼 Hyrox20260228 是在程式碼中硬編碼的
```

---

## ⚠️ 重要提醒

### **密碼安全性**

- ⚠️ **固定密碼對所有使用者相同**
- ⚠️ 這個設計適合**內部團隊使用**
- ⚠️ 不適合公開的生產環境

### **Firebase Console 設定**

確保已啟用 Email/Password 登入：

```
1. Firebase Console → Authentication
2. Sign-in method 標籤
3. 啟用 "Email/Password"
4. 儲存
```

---

## 🔒 安全性建議

### **目前的設計**

```
✅ 適合：內部團隊、測試環境
❌ 不適合：公開應用、生產環境
```

### **如果需要更高安全性**

可以考慮：

1. **動態密碼**：讓使用者自行設定密碼
2. **邀請碼**：需要邀請碼才能註冊
3. **Email 驗證**：發送驗證郵件
4. **IP 白名單**：限制特定 IP 存取

---

## 📊 資料結構

### **Firebase Authentication**

```typescript
{
  uid: "abc123",
  email: "dylan@example.com",
  displayName: "dylan",
  emailVerified: false,
  providerData: [
    {
      providerId: "password",
      email: "dylan@example.com"
    }
  ]
}
```

### **Auth Binding**

```typescript
{
  firebaseAuthUid: "abc123",
  appUserId: "u1",
  provider: "password",  // ← Email/密碼登入
  email: "dylan@example.com",
  displayName: "dylan",
  createdAt: Timestamp,
  updatedAt: Timestamp
}
```

---

## 🎯 使用場景

### **場景 1：Dylan 用 Email 登入**

```
1. 輸入：dylan@hyrox.com
2. 系統自動使用密碼：Hyrox20260228
3. 首次登入 → 自動建立帳號
4. 選擇 "Dylan"
5. 綁定完成
6. 進入應用
```

### **場景 2：Dylan 下次登入**

```
1. 輸入：dylan@hyrox.com
2. 系統自動使用密碼：Hyrox20260228
3. 登入成功
4. 檢查綁定 → 已綁定到 u1
5. 直接進入應用
```

### **場景 3：Dylan 用不同 Email**

```
1. 輸入：dylan.work@gmail.com
2. 系統自動使用密碼：Hyrox20260228
3. 首次登入 → 自動建立帳號
4. 選擇 "Dylan"（再次）
5. 建立新的綁定（不同 UID → 相同 appUserId）
6. 進入應用，看到相同的訓練記錄
```

---

## 📝 修改的檔案

```
🔧 修改：
├── src/components/LoginView.vue      # UI 更新
├── src/services/auth/index.ts        # 自動註冊邏輯
└── src/composables/useAuth.ts        # 移除匿名登入
```

---

## ✅ 檢查清單

- [x] 移除匿名登入按鈕
- [x] 新增 Email 輸入表單
- [x] 實作固定密碼登入
- [x] 實作自動註冊功能
- [x] 更新 UI 樣式
- [x] 移除 useAuth 中的匿名登入
- [x] 測試 Email 登入流程
- [x] 測試自動註冊流程

---

## 🚀 下一步

1. **在 Firebase Console 啟用 Email/Password 登入**
2. **測試完整的登入流程**
3. **（可選）調整密碼或新增更多安全機制**

---

## 💡 常見問題

### **Q: 使用者可以修改密碼嗎？**

A: 目前不行。密碼是固定的 `Hyrox20260228`，寫在程式碼中。

### **Q: 如果使用者忘記自己用哪個 Email 登入怎麼辦？**

A: 可以在 Firebase Console → Authentication → Users 查看所有註冊的 Email。

### **Q: 可以同時用 Google 和 Email 登入嗎？**

A: 可以！兩種方式都支援，且可以綁定到同一個應用使用者。

### **Q: 密碼會顯示在 UI 上嗎？**

A: 不會。密碼是在程式碼中硬編碼的，使用者看不到也無法修改。

---

## 🎉 總結

Email 登入功能已成功實作！現在使用者可以：

- ✅ 使用 Google 登入
- ✅ 使用 Email 登入（固定密碼）
- ✅ 首次登入自動建立帳號
- ✅ 選擇綁定到 Dylan/Crystal/Sylvi/Andrew

**固定密碼：`Hyrox20260228`**

有任何問題歡迎隨時詢問！🚀
